import "./App.css";

// Task 12 delivers the scaffold, typed API client, and design tokens only.
// The real UI (trip form, route map, log sheets, results dashboard) is
// built in Tasks 13-16 on top of the tokens defined in src/styles/tokens.css.
function App() {
  return <div className="app-shell" />;
}

export default App;
